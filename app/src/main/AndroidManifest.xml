<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rdseducation.english.grammer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_pdf"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_Grammer"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_AboutUs"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_Idoms"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_OneWord"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_Website"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_Rateus"
            android:screenOrientation="portrait"/>

        <service android:name=".firebase.FCMNotification">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <service android:name=".firebase.FCMService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>